apiVersion: v1
kind: Service
metadata:
  name: nodra-server
  namespace: nodra-system
spec:
  selector: {app.kubernetes.io/name: nodra, app.kubernetes.io/component: control-plane}
  ports:
    - {name: http, port: 8080, targetPort: http}
