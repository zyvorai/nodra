apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: nodra-server
  namespace: nodra-system
spec:
  maxUnavailable: 1
  selector:
    matchLabels: {app.kubernetes.io/name: nodra, app.kubernetes.io/component: control-plane}
